/*
 * *** Maja Swiderska ***
 *
 * This file contains two short practice problems using TreeSet/TreeMap.
 * Keep your code simple and readable. No extra methods are needed here.
 */

import java.util.*;

public class TreeProblems {

    /**
     * different()
     *
     * Given two sets of integers, return the set of elements that are in
     * exactly one of them (the symmetric difference).
     *
     * Examples:
     *  A = {3,6,7,9}, B = {2,4,6,7}  =>  {2,3,4,9}
     *
     * Return a TreeSet so the result is sorted ascending.
     */
    public static Set<Integer> different(Set<Integer> setA, Set<Integer> setB) {
        // TODO: implement using unions/intersections, or direct iteration.

        //I decided to create a new TreeSet that contains all elements from both of the sets
        //It gives me the union of setA and setB
        //A way I can explain this is if setA = {3,6,7,9} and setB = {2,4,6,7}
        //The final result would be {2,3,4,6,7,9} (all of the elements from setA and setB)
        Set<Integer> result = new TreeSet<>(setA);
        result.addAll(setB);

        //I need to find the intersection meaning I need to find elements that are in both sets
        //So I believe making a temporary set that contains setA then keep ony elements that are also in setB
        //A way to explain this is the intersection = {6,7}
        Set<Integer> intersection = new TreeSet<>(setA);
        intersection.retainAll(setB);

        //I need to remove the intersection from the result
        //Meaning it leaves only the elements that are in exactly in one set
        //A way to explain this would be the result was {2,3,4,6,7,9}, I remove {6,7} so the result is {2,3,4,9}
        result.removeAll(intersection);

        //Returning the symmetric difference
        return result;
    }

    /**
     * removeEven()
     *
     * Given a TreeMap<Integer,String>, remove all pairs where the key is even.
     * Modify the map in place. Keep this neat and safe (no concurrent mod errors).
     */
    public static void removeEven(Map<Integer, String> treeMap) {
        // TODO: implement (one simple approach uses keySet().removeIf(...))

        //I need to get all the keys from the TreeMap
        //The keySet() method returns a view of all the keys in the map
        Set<Integer> keys = treeMap.keySet();

        //I used removeIf with a lambda expression to remove even keys
        //Basically removeIf method does safely remove elements that match the condition
        //The condition in this case is = key % 2 == 0 checks if the key is divisible by 2
        //A way to explain this that if the keys = {1,13,15,16,17}, it will remove 10 and 16
        keys.removeIf(key -> key % 2 == 0);

        //The map is modified in place so no return value is needed in this case
        //The original treeMap now only contains entries with odd keys
        return;
    }
}
